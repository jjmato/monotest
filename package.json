{
  "name": "monotest",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {    
    "build": "npx lerna run build",
    "test": "npx lerna run test"
  },
  "devDependencies": {
    "lerna": "^5.4.0"
  }
}